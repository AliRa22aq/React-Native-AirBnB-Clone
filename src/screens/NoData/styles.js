import { StyleSheet, Dimensions } from "react-native";

const styles = StyleSheet.create({
    container: {
        height: "100%",
        width: '100%',
        justifyContent: 'center',
        alignItems: 'center'
    },
    text: {
        fontSize: 16,
        marginHorizontal: 20
    },
    button: {
        marginTop: 10,
        borderWidth: 1,
        width: '80%',
        height: 50,
        backgroundColor: '#5f3cfa',
        borderColor: '#676767',
        justifyContent: 'center',
        alignItems: 'center'
    }

});

export default styles;